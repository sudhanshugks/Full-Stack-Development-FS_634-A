import React from "react";

export default function App() {
  return (
    <div style={{ textAlign: "center", padding: 40, fontFamily: "sans-serif" }}>
      <h1>🚀 React + Multi-Stage Docker</h1>
      <p>Your app is running inside an optimized Docker container with Nginx.</p>
    </div>
  );
}
