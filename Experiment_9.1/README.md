# React Multi-Stage Docker

A lightweight React app built and deployed using a **multi-stage Dockerfile**.

### 🧩 Overview
- Stage 1: Node.js builds your React project.
- Stage 2: Nginx serves the optimized static files.
- Result: smaller image, faster runtime.

---

### 🚀 Run Locally

#### 1. Install dependencies
```bash
npm install
